/**
 * インタースティシャル（全画面広告）の表示間隔まわり。
 * 数値はリリース後の体裁に合わせてここだけ変えればよいように集約。
 */

/** 食事追加が何件続いたらインタースティシャルを 1 回試みるか */
export const FOOD_ADDS_PER_INTERSTITIAL = 3;

/**
 * 食事経由の表示で用いる最低インターバル（ms）。直前に別のインターを出した直後の連打を避ける。
 * ワークアウト完了は別途 bypass する。
 * 0 にすると食事側のクールダウンは無効。
 */
export const INTERSTITIAL_MIN_INTERVAL_MS = 60_000;
